#include <stdio.h>
#include <stdlib.h>

void main()
{
// definindo variaveis
    int a = 1, b = 10;

// contando até 10 ------------
    while(a <= 10){
//imprimindo 'a' na tela:
    printf("\n %d", a);

//incremento
    a++; //a = a + 1; (a++ ou -- é a mesma coisa que a = a+1
}
    a = 1;

    do {
    //imprimindo 'a' na tela:
        printf("\n %d", a);

    //incremento
        a++; //a = a + 1; (a++ ou -- é a mesma coisa que a = a+1
    }
    while(a <= 10);













/*
// Contagem regressiva ----------
 while(b >= 1){
//imprimindo 'b' na tela:
    printf("\n %d", b);

//incremento
    b = b - 2; // quando for de dois em dois não pode usar o ++
}

*/
}

// Laços de Repetiçoes While (enquanto)
